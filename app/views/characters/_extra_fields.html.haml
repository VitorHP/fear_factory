.nested-fields
  = f.input :id, as: :hidden
  = f.input :name, label: false, placeholder: t("activerecord.attributes.extra.name")
  = f.input :description, label: false, placeholder: t("activerecord.attributes.extra.description")
  = link_to_remove_association f do
    .fa.fa-times
